Frontend helper wrappers will live here.
Use this for safe client utilities and SDK wrappers when tasks require them.
Do not hardcode pricing, roles, production URLs, or secrets here.
Shared contracts belong in `packages/shared`.
